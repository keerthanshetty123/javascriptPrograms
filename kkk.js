 import React, { useState } from 'react'; const CharacterCountTextarea = () => { const [text, setText] = useState(''); const handleChange = (event) => { setText(event.target.value); }; return ( <div> <textarea rows="4" cols="50" value={text} onChange={handleChange} placeholder="Type something here..." /> <div> Character Count: {text.length} </div> </div> ); }; // Example usage const App = () => { return ( <div> <h1>Character Count Example</h1> <CharacterCountTextarea /> </div> ); }; export default App;